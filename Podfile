# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'simple_weather_project' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for simple_weather_project

  target 'simple_weather_projectUITests' do
    # Pods for testing
    pod 'NMSSH'
  end

end
